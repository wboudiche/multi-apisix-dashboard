# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://help.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"

  - package-ecosystem: "npm"  # See documentation for possible values
    directory: "/"  # Location of package manifests
    schedule:
      interval: "daily"
    groups:
      # react and react-dom are separate npm packages, so Dependabot bumps them
      # in separate PRs — and React refuses to run when the two versions differ
      # at all, throwing "Incompatible React versions" before anything renders.
      # The page comes up blank, every spec that drives the UI fails and the
      # API-only ones still pass. PR #117 did exactly this and cost issue #121;
      # PR #123 repeated it one version lower.
      #
      # Nothing catches it on the way in: react-dom's peer range accepts a newer
      # react, so the install is silent. React's own check exists only in its
      # development build, so a skewed pair would ship to production without a
      # word — src/react-version-pairing.test.ts states the invariant instead.
      #
      # Two entries because a group applies to one update type: without the
      # second, a React security advisory would still arrive as a single-package
      # PR and reintroduce the skew this exists to prevent.
      #
      # @types/react and @types/react-dom are here for the same reason one step
      # removed: @types/react-dom peers on @types/react, and a lone bump of
      # either breaks tsc rather than the browser.
      react:
        applies-to: version-updates
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      react-security:
        applies-to: security-updates
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      # Both of these were evaluated and declined on their merits, not missed.
      # Without these entries Dependabot reopens them on every release and the
      # same reasoning has to be redone each time.
      #
      # monaco-editor: 0.53 moved the nested language namespaces to the top
      # level (languages.json -> json) and 0.55 added an exports map, so the
      # deep imports in src/utils/monaco.ts no longer resolve. The quick fix is
      # to import the package root, which pulls in the css, html and typescript
      # language services the dashboard never uses: +141 kB gzipped on the
      # editor chunk. Doing it properly means keeping the minimal imports and
      # working around where the types now live. Worth doing the day a specific
      # fix from a newer version is needed — see #43.
      - dependency-name: "monaco-editor"
        update-types:
          ["version-update:semver-major", "version-update:semver-minor"]
      # eslint-plugin-react-refresh: 0.5 makes only-export-components far
      # stricter, taking the repo from 90 lint problems to 174. All 84 new ones
      # are warnings, but lint-staged runs with --max-warnings=0, so every one
      # of the ~40 affected files becomes uncommittable until it is restructured
      # or suppressed. That is a large refactor for a dev-only rule with no
      # runtime benefit — see #41.
      - dependency-name: "eslint-plugin-react-refresh"
        update-types:
          ["version-update:semver-major", "version-update:semver-minor"]

  - package-ecosystem: "gomod"  # See documentation for possible values
    directory: "/api"  # Go module lives in api/, not repo root
    schedule:
      interval: "daily"
